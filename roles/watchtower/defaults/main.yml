#SPDX-License-Identifier: MIT-0
---
# defaults file for vpn_servers

required_packages:
  - docker.io
  - docker-compose
  
# Parameters for repository update control
update_repo: true
force_repo_update: true

# Common paths
PWD: "/server"
watchtower_path: "{{ PWD }}/watchtower"